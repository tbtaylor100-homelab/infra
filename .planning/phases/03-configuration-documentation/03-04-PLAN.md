---
phase: 03-configuration-documentation
plan: "04"
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
  - "03-03"
files_modified: []
autonomous: false
requirements:
  - CONF-01
  - CONF-02
  - DOC-01

must_haves:
  truths:
    - "homelab-knowledge artifacts (setup-aiostreams.md + ADR-017) committed and pushed to ssh://git@192.168.1.50:2222/tbtaylor100/homelab-knowledge.git"
    - ".planning/runbooks/validate-filter.md committed and pushed to infra repo"
    - "Operator has confirmed Real-Debrid shows as Connected in AIOStreams UI"
    - "Operator has confirmed WEB-DL/YTS/AMZN streams do NOT appear in Stremio search results"
  artifacts:
    - path: "../homelab-knowledge/runbooks/setup-aiostreams.md"
      provides: "Committed operational runbook in homelab-knowledge repo"
      contains: "# Runbook: Set Up AIOStreams"
    - path: "../homelab-knowledge/adr/ADR-017-aiostreams-stremio-filtering.md"
      provides: "Committed ADR in homelab-knowledge repo"
      contains: "# ADR-017"
    - path: ".planning/runbooks/validate-filter.md"
      provides: "Committed validation procedure in infra repo"
      contains: "# Filter Validation Procedure"
  key_links:
    - from: "homelab-knowledge git"
      to: "ssh://git@192.168.1.50:2222/tbtaylor100/homelab-knowledge.git"
      via: "git push origin main"
      pattern: "2 files changed"
    - from: "infra git"
      to: "http://forgejo.local:3000/root/infra.git"
      via: "git push origin main"
      pattern: ".planning/runbooks/validate-filter.md"
---

<objective>
Commit all Phase 3 documentation artifacts to their respective git repositories, push to Forgejo remotes, and confirm that filter validation passes via a human operator checkpoint (Stremio UI verification).

Purpose: Wave 1 plans wrote the documentation files. This Wave 2 plan activates them: commits homelab-knowledge artifacts (setup-aiostreams.md + ADR-017) to the homelab-knowledge repo and .planning/runbooks/validate-filter.md to the infra repo, then verifies the filter is actually working via the human checkpoint.

Output: All three Phase 3 deliverables committed and pushed; filter validation confirmed by operator.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-configuration-documentation/03-CONTEXT.md
@.planning/phases/03-configuration-documentation/03-VALIDATION.md
@.planning/phases/03-configuration-documentation/03-01-SUMMARY.md
@.planning/phases/03-configuration-documentation/03-02-SUMMARY.md
@.planning/phases/03-configuration-documentation/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit and push homelab-knowledge artifacts</name>
  <files></files>
  <read_first>
    - .planning/phases/03-configuration-documentation/03-CONTEXT.md (code_context: homelab-knowledge is a separate repo at ../homelab-knowledge/, requires separate commit and push to ssh://git@192.168.1.50:2222/tbtaylor100/homelab-knowledge.git)
    - .planning/phases/03-configuration-documentation/03-01-SUMMARY.md (confirm setup-aiostreams.md was created)
    - .planning/phases/03-configuration-documentation/03-03-SUMMARY.md (confirm ADR-017 was created)
  </read_first>
  <action>
    From the homelab-knowledge repo directory (../homelab-knowledge/ relative to infra repo):

    1. Verify both files exist:
       `ls homelab-knowledge/runbooks/setup-aiostreams.md`
       `ls homelab-knowledge/adr/ADR-017-aiostreams-stremio-filtering.md`
       Stop and report if either is missing — do not commit partial artifacts.

    2. Verify no credentials committed:
       `grep -rn 'apiKey=\|SECRET_KEY=[a-f0-9]' homelab-knowledge/runbooks/setup-aiostreams.md homelab-knowledge/adr/ADR-017-aiostreams-stremio-filtering.md`
       Expected: no output. If any output, stop and report before committing.

    3. Stage both files (from the homelab-knowledge/ directory):
       `git -C ../homelab-knowledge add runbooks/setup-aiostreams.md adr/ADR-017-aiostreams-stremio-filtering.md`

    4. Commit with message:
       `git -C ../homelab-knowledge commit -m "docs(aiostreams): add setup runbook and ADR-017"`

    5. Push to Forgejo remote:
       `git -C ../homelab-knowledge push origin main`
       Remote: ssh://git@192.168.1.50:2222/tbtaylor100/homelab-knowledge.git

    6. Verify push succeeded:
       `git -C ../homelab-knowledge log --oneline -1`
       Expected: shows the new commit.
  </action>
  <verify>
    <automated>git -C /Users/thomas.taylor/Documents/github/homelab-knowledge log --oneline -1 2>/dev/null | grep -c 'aiostreams'</automated>
  </verify>
  <acceptance_criteria>
    - homelab-knowledge/runbooks/setup-aiostreams.md exists and is tracked in homelab-knowledge git
    - homelab-knowledge/adr/ADR-017-aiostreams-stremio-filtering.md exists and is tracked in homelab-knowledge git
    - No credential values in either committed file
    - git log -1 in homelab-knowledge shows commit message "docs(aiostreams): add setup runbook and ADR-017"
    - git status in homelab-knowledge shows working tree clean after push
  </acceptance_criteria>
  <done>homelab-knowledge commit exists with both files; git status clean; no credential values committed</done>
</task>

<task type="auto">
  <name>Task 2: Commit and push validate-filter.md to infra repo</name>
  <files>.planning/runbooks/validate-filter.md</files>
  <read_first>
    - .planning/phases/03-configuration-documentation/03-02-SUMMARY.md (confirm validate-filter.md was created at .planning/runbooks/validate-filter.md)
  </read_first>
  <action>
    From the infra repo directory:

    1. Verify file exists:
       `ls .planning/runbooks/validate-filter.md`
       Stop and report if missing.

    2. Stage the file:
       `git add .planning/runbooks/validate-filter.md`

    3. Commit with message:
       `git commit -m "docs(phase-03): add filter validation procedure"`

    4. Push to Forgejo:
       `git push origin main`
       Remote: http://forgejo.local:3000/root/infra.git

    5. Verify:
       `git log --oneline -1`
       Expected: shows the new commit including validate-filter.md.
  </action>
  <verify>
    <automated>git log --oneline -1 2>/dev/null | grep -c 'validate-filter\|phase-03\|filter'</automated>
  </verify>
  <acceptance_criteria>
    - .planning/runbooks/validate-filter.md is committed to infra repo main branch
    - git log --oneline -1 shows the docs(phase-03) commit
    - git status shows working tree clean
  </acceptance_criteria>
  <done>.planning/runbooks/validate-filter.md committed to infra main and pushed to Forgejo</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plans 01-03 wrote three documentation artifacts:
    - homelab-knowledge/runbooks/setup-aiostreams.md — AIOStreams post-deploy UI setup runbook (CONF-01)
    - .planning/runbooks/validate-filter.md — filter validation procedure (CONF-02)
    - homelab-knowledge/adr/ADR-017-aiostreams-stremio-filtering.md — deployment ADR (DOC-01)

    Tasks 1-2 committed and pushed all artifacts to their respective repositories.

    Now: follow the setup-aiostreams.md runbook to configure AIOStreams, then run the validate-filter.md procedure to confirm the regex filter is blocking WEB-DL/YTS/AMZN streams in Stremio.
  </what-built>
  <how-to-verify>
    **Step A — AIOStreams UI setup (setup-aiostreams.md)**

    1. Open AIOStreams UI at http://192.168.1.205:3000 from a LAN device
    2. Navigate Services → Real-Debrid → verify "Connected" status (green checkmark or Connected text)
    3. Navigate Addons → Marketplace → search "Torrentio" or paste https://torrentio.strem.fun/manifest.json → Install
    4. Navigate Filters → Regex Patterns → enable the "Use Regex Filters" toggle (patterns are pre-configured)
    5. In Stremio: Settings → Addons → Add source: http://192.168.1.205:3000/manifest.json

    If Real-Debrid shows Disconnected:
    - Run: kubectl get externalsecret -n aiostreams aiostreams-secret (expect READY: True)
    - Run: kubectl rollout restart deployment aiostreams -n aiostreams (force env var re-read)
    - Wait 30s and refresh AIOStreams UI

    **Step B — Filter validation (validate-filter.md)**

    1. In Stremio, search for: "Breaking Bad" or "Oppenheimer" or any popular title with WEB-DL releases
    2. Inspect stream results — no WEB-DL, AMZN, DSNP, YTS, RARBG, or EZTV labels should appear
    3. If results look stale: Settings → Addons → Remove AIOStreams source → Re-add http://192.168.1.205:3000/manifest.json

    Phase 3 success criteria (from ROADMAP.md):
    - Stremio client shows AIOStreams as an addon source at http://192.168.1.205:3000
    - Real-Debrid shows as Connected in AIOStreams Services UI
    - Test search in Stremio for WEB-DL/YTS/AMZN content returns NO filtered-out streams
    - homelab-knowledge ADR-017 is committed (confirmed in Tasks 1-2)
  </how-to-verify>
  <resume-signal>
    If both setup and validation pass: type "validated" and I will mark Phase 3 complete and update STATE.md.
    If Real-Debrid shows Disconnected: describe the ExternalSecret status output — I will diagnose.
    If WEB-DL entries still appear: describe what you see in Stremio — I will diagnose the filter activation.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| infra git → Forgejo | Commit pushed to http://forgejo.local:3000/root/infra.git; ArgoCD does NOT sync .planning/ directory |
| homelab-knowledge git → Forgejo | Commit pushed to ssh://git@192.168.1.50:2222/tbtaylor100/homelab-knowledge.git; separate repo, separate CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Information Disclosure | git commits (both repos) | mitigate | Credential check in Task 1 before commit. Verify: git show --stat HEAD in homelab-knowledge shows only .md files, no .env, no secret values. |
| T-03-06 | Availability | homelab-knowledge push to SSH remote | accept | SSH key must be configured for git@192.168.1.50. If push fails with auth error: verify SSH key in ~/.ssh/ grants access to Forgejo at 192.168.1.50:2222. Non-blocking for infra repo — can commit infra repo independently. |
</threat_model>

<verification>
Phase 3 complete when ALL of the following are true:

1. `git -C ../homelab-knowledge log --oneline | grep 'aiostreams'` shows at least one commit
2. `ls ../homelab-knowledge/runbooks/setup-aiostreams.md` — file exists
3. `ls ../homelab-knowledge/adr/ADR-017-aiostreams-stremio-filtering.md` — file exists
4. `ls .planning/runbooks/validate-filter.md` — file exists
5. Operator confirms: Real-Debrid shows Connected in AIOStreams UI at http://192.168.1.205:3000
6. Operator confirms: Stremio search for WEB-DL content returns zero WEB-DL/AMZN/YTS entries
</verification>

<success_criteria>
- homelab-knowledge/runbooks/setup-aiostreams.md: committed and pushed, no credentials
- homelab-knowledge/adr/ADR-017-aiostreams-stremio-filtering.md: committed and pushed, no credentials
- .planning/runbooks/validate-filter.md: committed and pushed to infra repo
- Operator checkpoint passed: Real-Debrid Connected in UI + Stremio search shows no WEB-DL/AMZN/YTS entries
</success_criteria>

<output>
After completion, create .planning/phases/03-configuration-documentation/03-04-SUMMARY.md
</output>
