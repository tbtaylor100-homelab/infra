# OpenTofu / Terraform
**/.terraform/
*.tfstate
*.tfstate.backup
*.tfstate.*.backup
*.tfstate.lock.info
.terraform.lock.hcl
*.tfvars.secrets
crash.log

# Secrets
.env
*.pem
*.key
*.secrets.yml
secrets.yml

# OS
.DS_Store

# Claude Code local settings
.claude/settings.local.json

# MCP server config (machine-specific local paths)
.mcp.json

# RepoWise local index (regenerable via `repowise init`)
.repowise/
