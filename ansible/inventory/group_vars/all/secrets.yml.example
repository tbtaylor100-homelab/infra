# Copy this file to secrets.yml and fill in real values.
# secrets.yml is gitignored — never commit it.
# Future: these will be sourced from OpenBao (see JIRA epic).

ubuntu_password: "change-me"

# MCP server secrets
forgejo_mcp_token: "your-forgejo-token-here"
jira_username: "your-email@example.com"
jira_api_token: "your-jira-api-token-here"
proxmox_mcp_token_name: "mcp"
proxmox_mcp_token_value: "your-proxmox-token-uuid-here"
forgejo_runner_token="your-token-here"